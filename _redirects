# ── ArtDrop redirects only ──────────────────────────────────────
# No cross-project redirects. GlowUpWorks handles its own routing.
# getbizglow.com DNS should point to glowupworks.com Netlify, not here.
